#ifndef MATRIX_SIZE_H
#define MATRIX_SIZE_H
#include <stdlib.h>

/*
*   with this structure we will know
*   exact number of rows and columns in matrice
*/
typedef struct {
    size_t rows;
    size_t cols;
} matrix_size;

#endif // MATRIX_SIZE_H
